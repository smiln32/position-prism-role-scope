# Session Handoff — 2026-07-17

Zero-context resume snapshot. Supersedes `HANDOFF-2026-07-16.md`. Read
`CLAUDE.md` + `CONTEXT.md` (repo root) first, then `STATE.md`, `DECISIONS.md`
(all 2026-07-17 entries), `NEXT-STEPS.md`.

## Repo / branch state

- **`master`**: green at PR #12 (docs + defect fixes). 131 tests there.
- **PR #13 OPEN**: `feature/role-interviews` — the pre-pilot batch (role
  interviews, deletion caution + CUSTODY.md, real PDF export via pdfmake,
  role deliverables, operator workbench). 154 tests. Pushed.
- **`feature/assisted-interviewing`** (STACKED on role-interviews, pushed,
  no PR yet): the revived LLM adapter. **166 tests + 1 guarded live test.**
  HEAD = current state. Merging this brings PR #13's commits too — either
  merge #13 first, or open one PR from this branch for both.
- Suite/lint/build all green at HEAD. Frozen schema untouched all week.
- GitHub repo renamed: `smiln32/position-prism-role-scope` (old `successor`
  URLs redirect). Local remote updated.

## The product model (owner decisions, memory files exist for all)

Service business: HE operates the tool, interviews the client's people
(ROLE-HOLDER FIRST — the person who does the job; owner is fallback +
verifier), delivers reports/PDFs; every company keeps its data (CUSTODY.md
protocol: vault first, export, verify restore, attested delete). Role
descriptions are the product — generics suffice, names are attribution.
**Two integral paths, never "bolted on" (owner's framing, use it): the
LOCAL path (operator structures by hand via the workbench) and the ASSISTED
path (model drafts, operator reviews, owner confirms) — client constraint
picks the path; "nothing leaves this room" = local path, priced for the
hours.**

## Assisted interviewing (built 2026-07-17, ONE STEP REMAINS)

`llm.ts`: enrichment layer — floor SAVED before any network call; drafts
across all 7 non-interview entity types (inferred/low/unverified); ≤3
clarification flags per answer queued as next-sitting follow-up threads;
claude-haiku-4-5; key memory-only via home-screen panel ("Turn off and
forget"). Question-rewording dropped deliberately.
**REMAINING: the owner's live run — he HAS an API key (in his password
manager, never in chat/repo).** PowerShell, in `app/`:
`$env:ANTHROPIC_API_KEY="sk-ant-…"; npm test -- llm.live; Remove-Item
Env:ANTHROPIC_API_KEY` — or `npm run dev` → paste key in Assisted panel →
answer a few questions. Pass ⇒ merge.

## Open decision: the Role Package (reports)

Proposed, NOT yet approved: three new renderers per role project — **Job
Description, SOPs (one per process), Training/Onboarding Guide** (+ fold in
a Commitment Register). Cheap: renderer + `Doc.c()` each, audit-clean, PDF
free. Rationale: his friend's accounts-payable test (product test, not
pilot; genericized answers — role level, no vendor names) should end with
those in her hands. Org-level reports (SPOF register, contradiction map,
access inventory) need the multi-model reader — DO NOT use `mergeModels`
for it (merges by id, loses who-said-what); deferred until a multi-person
engagement exists.

## Owner's own parallel items

Exit-planner conversation (market test); engagement letter with lawyer
(custody, keep-nothing vs keep-encrypted, assisted-path disclosure citing
Anthropic's commercial data-use terms); friend's AP test; then the paid
pilot. Structuring-hours measurement now informs (not gates) decisions.

## How to resume

```
$env:Path = "C:\Program Files\nodejs;" + $env:Path
git checkout feature/assisted-interviewing   # current state
cd app && npm install && npm test            # expect 166 passed, 1 skipped
```
Non-negotiables: CLAUDE.md (root) + provenance-says-who-typed-it +
floor-before-network. Never build on a broken baseline.
