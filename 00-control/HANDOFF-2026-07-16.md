# Session Handoff — 2026-07-16

A point-in-time snapshot so the next session (human or AI) can resume with zero
verbal context. Supersedes `HANDOFF-2026-07-15.md`. Read `CLAUDE.md` and
`CONTEXT.md` (repo root) before writing code; the living pick-up note is
`NEXT-STEPS.md`; the roadmap is `EXPANSION-HANDOFF.md`.

---

## Repo state at handoff

- **Resolved 2026-07-17:** both branches were pushed and merged to `master`
  via **PR #12**, so `master` is the current state — resume from it. The
  branch descriptions below are kept as the record of what merged:
  - `docs/workspace-icm-and-root-claude-2026-07-16` — 3 commits (CLAUDE.md to
    root, CONTEXT.md stage map, 06-export docs, ICM audit, area-count fix,
    EXPANSION-HANDOFF refresh).
  - `fix/provenance-and-extraction-defects` — stacked on it, 7 more commits
    (P1/P3/P4 then P5/P6/P7 fixes, doc-staleness cleanup, PR-archival records,
    adapter revival scope, this handoff).
- Note: the GitHub repo has been **renamed** — pushes report
  `github.com/smiln32/position-prism-role-scope` (old `successor` URLs
  redirect). Docs referencing the old name still work but are dated.
- **Suite: 131 tests green** (was 106 at session start), lint clean, build
  clean (69.02 kB gzip). Frozen `schema.ts` untouched all session.
- **No open PRs.** #3 and #8 were CLOSED on GitHub 2026-07-16, owner-directed;
  both branches retained on origin. #3 is deferred-not-rejected — revive by
  PORTING onto master, never merging (it predates PRs #4–#9; ~4,300 deletions).
- Owner's untracked root doc (`Business Knowledge Succession Platform.md`)
  remains untracked by design.

## The business decisions this session (bigger than the code)

1. **Successor is a SERVICE, not shipped software.** The owner runs the tool,
   interviews the client's people, delivers reports and documents. **Every
   company keeps its own data.** This dissolves most of PATH-TO-SHIP: ship
   decision #1 (website vs. desktop) is moot, #3 (adapter in v1) is decided
   (deferred), and free-vs-paid resolves to invoicing.
2. **The market is any key person leaving** — a retiree, a new department's
   roles, not just owner exits. That is Role DNA; the `subjectRole` seam exists
   in the schema but is inert (hardcoded 'owner', read by nothing).
3. **Cross-company benchmark data: parked**, constraints settled (data-use
   clause not NDA; content can't be anonymized, derived structure can).
4. Persistent memory files exist for all three (`~/.claude/.../memory/`).

## What the code session fixed (all logged in DECISIONS.md 2026-07-16)

- **P1** — provenance depends on who is typing: capture.ts takes an
  Attribution; operator entries are inferred/medium/unverified and name who
  structured them. KnowledgeScreen asks "Who is entering this?". The
  setVerified() promotion path is now meaningful.
- **P3** — the May bug: month matching is word-boundary, case-sensitive for
  March/May/August.
- **P4** — name-gap noise: whole-name grouping, profile names known, ALL-CAPS
  skipped, 25/document cap with reported overflow. Fixture: 11 gaps → 3.
- **P5** — Track 7 answers now become owner-declared RiskEntities (verbatim,
  source 'interview', dismissals excluded). Risk scores finally have spread.
- **P6** — handbook renders document lines grouped per document, as bullets.
- **P7** — renderers distinguish "This part of the interview has not been
  asked yet." from "Not yet captured." via interviewMemory.
- Plus: living-doc staleness cleanup (phantom "existing" AI adapter claim,
  embedded test counts, HELP.md's missing passphrase/file-type notes).

## Prioritized next actions

1. ~~Push the branches / merge to master~~ — **done 2026-07-17, PR #12.**
2. **Pilot on one real business** — the next milestone for everything. The
   product has never touched a live owner; the 50 areas are theory. Do the
   structuring pass BY HAND and time it — that measurement decides #3's
   revival (see EXPANSION-HANDOFF §6A for the port-don't-merge scope:
   commitments+systems extraction, clarification flagging).
3. **Real PDF export** — the report IS the product in the service model;
   browser print-to-PDF is not a client deliverable.
4. Deferred, needs the owner: P2-A engagement types (subjectRole knob, track
   subsets, completion semantics), the org rollup (multi-model renderer — do
   NOT use mergeModels for it; it merges by id and loses who-said-what).

## How to resume

```
$env:Path = "C:\Program Files\nodejs;" + $env:Path   # node/npm not on PATH here
git checkout master && git pull                       # current state (post PR #12)
cd app && npm install && npm run build && npm test    # expect 131 passing
npm run lint
```
`gh` is at `C:\Program Files\GitHub CLI\gh.exe`, authed as `smiln32`.
Read `CLAUDE.md` + `CONTEXT.md` (root), then `00-control/`: `STATE.md`,
`DECISIONS.md` (the 2026-07-16 entries carry this whole session),
`NEXT-STEPS.md`, `EXPANSION-HANDOFF.md`, `PATH-TO-SHIP.md`. Never build on a
broken baseline.

## Non-negotiables (do not regress)

Everything in `CLAUDE.md`, plus one sharpened this session: **provenance says
who actually entered it.** "Entered directly by the owner" is only ever true
when the owner was at the keyboard; operator-structured knowledge is inferred,
unverified, and names its author until the owner confirms it. Never fabricate;
labeled inference only; never delete captured knowledge; no stored credentials;
disclaimer everywhere; `auditRendered` stays green; frozen `schema.ts` needs a
versioned, logged decision.
