# Session Handoff — 2026-07-24

Zero-context resume snapshot. Supersedes `HANDOFF-2026-07-17.md` (which lives
only on `feature/assisted-interviewing`; master had carried `HANDOFF-2026-07-16.md`
until this file). Read `CLAUDE.md` + `CONTEXT.md` (repo root) first, then
`STATE.md`, `DECISIONS.md` (2026-07-23 entry), `NEXT-STEPS.md`.

## Repo / branch state

- **`master`**: green at `bc875d7`. **160 tests**, 16 files. Three PRs merged
  this session — **#13** role interviews, **#14** the Role Package, **#15**
  session tooling.
- **`feature/assisted-interviewing`** (`c4dd99d`, pushed): the LLM adapter —
  the ONLY open feature branch. Rebased this session to drop the origin-prompt
  commit, then force-pushed. It still carries redundant copies of the Role
  Package + tooling (now on master under different SHAs) — see "Merging the
  assisted branch" before merging it.
- Merged branches kept (no-delete convention): `feature/role-interviews`,
  `feature/role-package`, `feature/session-tooling`.
- GitHub repo: `smiln32/position-prism-role-scope`.

## What shipped to master this session (2026-07-23 → 24)

- **The Role Package** (PR #14; `DECISIONS.md` 2026-07-23): three role-only
  deliverables — **Job Description** (with a Commitment Register folded in),
  **Standard Operating Procedures** (one per process), **Training & Onboarding
  Guide**. `deliverablesFor()` appends them for non-owner projects only; owner
  projects are byte-for-byte unchanged; the zero-invention audit covers them
  unchanged; a new `quoteAreas()` helper gives each grouped section one honest
  empty-state line. 154 → 160 tests.
- **Session tooling** (PR #15): `where-we-stand.ps1` (regenerable
  `WHERE-WE-STAND.md` snapshot; the output is gitignored) and `push-log.ps1`
  + `00-control/PUSH-LOG.md` (reversible push ledger; `CONTEXT.md` routes to it).
- **The original build prompt** was archived into the repo, then removed at the
  owner's request — it now lives ONLY on the owner's laptop at
  `c:/Users/smiln/Downloads/apps/ORIGIN-PROMPT.md`, and is gone from every
  branch (local and remote).

## Assisted interviewing — ONE STEP REMAINS (unchanged)

Built and unit-tested: `llm.ts` enrichment layer — the verbatim Fact floor is
SAVED before any network call; drafts across all 7 non-interview entity types
(inferred / low / unverified); ≤3 clarification flags per answer queued as
next-sitting threads; key is memory-only via a home-screen panel;
`claude-haiku-4-5`. Question-rewording was dropped deliberately (operator reads
questions aloud).

**REMAINING: the owner's live-key run.** PowerShell, in `app/`:
```
$env:ANTHROPIC_API_KEY="sk-ant-…"; npm test -- llm.live; Remove-Item Env:ANTHROPIC_API_KEY
```
The key lives in the owner's password manager — never in chat or the repo.
Pass ⇒ ready to merge.

## Merging the assisted branch when the live run passes

Do NOT merge `feature/assisted-interviewing` as-is: it re-contains the Role
Package + session tooling (already on master under different SHAs), so a
straight PR would conflict on `DECISIONS.md` / `STATE.md`. Use the same split
that shipped the Role Package cleanly (see `DECISIONS.md` 2026-07-23): branch
off master and bring only the LLM-adapter files —
`app/src/interview/llm.ts`, `llm.test.ts`, `llm.live.test.ts`,
`InterviewScreen.assist.test.tsx`, `App.tsx`, `InterviewScreen.tsx`,
`02-interview/README.md` — then write a fresh `DECISIONS.md` entry. Verify
`App.tsx` / `InterviewScreen.tsx` merge cleanly against master's
role-interviews versions (both PR #13 and the adapter touched them).

## The product model (standing owner decisions)

Service business: the owner operates the tool, interviews the client's people
(**role-holder first** — the person who does the job; owner is fallback +
verifier), delivers reports/PDFs; every company keeps its data (`08-docs/
CUSTODY.md`). Two integral paths, never "bolted on": the **local** path
(operator structures by hand via the workbench) and the **assisted** path
(model drafts, operator reviews, owner confirms); "nothing leaves this room"
= the local path.

## Owner's own parallel items (the real-world track)

Exit-planner conversation (market test); engagement letter with the lawyer
(custody, keep-nothing terms, assisted-path disclosure); the friend's
accounts-payable test (product test, genericized answers); then the paid pilot.
Org-level reports (SPOF register, contradiction map, access inventory) remain
DEFERRED — they need the multi-model reader, never `mergeModels`.

## New tooling now on master

- `./where-we-stand.ps1` `[-RunTests]` — regenerates `WHERE-WE-STAND.md`
  (git state, current stage, pointers; the file is gitignored).
- `./push-log.ps1` `[-DryRun]` — pushes AND records a reversible entry in
  `00-control/PUSH-LOG.md` (before-sha, durable anchor tag, one-line undo).
  `-DryRun` previews without touching anything.

## How to resume

```
$env:Path = "C:\Program Files\nodejs;" + $env:Path
git checkout master
cd app && npm install && npm run build && npm test   # expect 160 passing
npm run lint
```
Non-negotiables: `CLAUDE.md` (root); never fabricate; labeled inference only;
provenance says who typed it; floor before network; the zero-invention audit
must keep passing; the frozen schema stays untouched without a logged decision.
